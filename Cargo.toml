[workspace]
resolver = "2"
members = [
    "bitmap-font",
    "font-atlas",
    "web",
]

# Shared dependencies can be defined here to ensure version consistency
[workspace.dependencies]
compact_str = "0.9.0"
thiserror = "2.0.12"
image = "0.25.6"
